import { ReturnsFeature } from '@/features/modules/rental/returns/ReturnsFeature';

export const metadata = {
  title: 'Pengembalian Kendaraan | ADATRACK',
};

export default function ReturnsPage() {
  return <ReturnsFeature />;
}
